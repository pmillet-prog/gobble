// Generated by scripts/import-avatar-workshop.mjs from the avatar workshop.

export default (function () {
  'use strict';
  function draw(ctx, assets, state, width, height) {
    if (!state.backdrops) return false;
    const image = assets['backdrops_' + state.backdrops] || assets['backdrops_' + state.backdrops + '_thumbnail'];
    if (!image) return false;
    const ratio = Math.max(width / image.width, height / image.height);
    ctx.save(); ctx.beginPath(); ctx.rect(0, 0, width, height); ctx.clip();
    ctx.drawImage(image, (width - image.width * ratio) / 2, (height - image.height * ratio) / 2, image.width * ratio, image.height * ratio);
    if (/^#[0-9a-f]{6}$/i.test(state.backdropColor || '')) {
      ctx.globalAlpha = Math.max(0, Math.min(.5, Number(state.backdropTint) || 0));
      ctx.fillStyle = state.backdropColor; ctx.fillRect(0, 0, width, height);
    }
    ctx.restore(); return true;
  }
  return { draw };
})();
