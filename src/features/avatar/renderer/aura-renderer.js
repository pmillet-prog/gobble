// Generated by scripts/import-avatar-workshop.mjs from the avatar workshop.

export default (function () {
  'use strict';
  // Called in avatar coordinates before every character layer. Palette identifies the reward.
  function draw(ctx, assets, state) {
    if (!state.auras) return false;
    const art = assets['auras_' + state.auras] || assets['auras_' + state.auras + '_thumbnail'];
    if (!art) return false;
    ctx.drawImage(art, 0, 0, 1024, 1024);
    return true;
  }
  return { draw };
})();
